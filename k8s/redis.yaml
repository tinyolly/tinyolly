# ARCHIVED: Redis is no longer the storage backend for TinyOlly.
# SQLite (tinyolly-sqlite-pvc.yaml) is used instead.
# This file is retained for reference. Do NOT apply it — it is not used by any deploy script.
# See docs/redis-archive.md for migration details.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tinyolly-redis
  labels:
    app: tinyolly-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tinyolly-redis
  template:
    metadata:
      labels:
        app: tinyolly-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        command: 
        - redis-server
        - --port
        - "6579"
        - --maxmemory
        - "256mb"
        - --maxmemory-policy
        - "allkeys-lru"
        ports:
        - containerPort: 6579
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - redis-cli
            - -p
            - "6579"
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - redis-cli
            - -p
            - "6579"
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: tinyolly-redis
spec:
  selector:
    app: tinyolly-redis
  ports:
    - protocol: TCP
      port: 6579
      targetPort: 6579
